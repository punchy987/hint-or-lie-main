# Configuration environnement - Hint or Lie
# Copiez ce fichier en `.env` et remplissez les valeurs

# Port du serveur (défaut : 5500)
PORT=5500

# Environnement (development ou production)
NODE_ENV=development

# Configuration Firebase (optionnel - pour la persistance des scores)
# En production sur Render, ajoutez le contenu complet du fichier JSON Firebase
# FIREBASE_CONFIG={"type":"service_account","project_id":"..."}

# En développement, le fichier config/firebase-service-account.json sera utilisé automatiquement
